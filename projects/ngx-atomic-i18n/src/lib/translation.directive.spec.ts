import { TranslationDirective } from './translation.directive';

describe('TranslationDirective', () => {
  it('should create an instance', () => {
    const directive = new TranslationDirective();
    expect(directive).toBeTruthy();
  });
});
