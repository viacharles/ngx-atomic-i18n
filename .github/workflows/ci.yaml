name: CI

on:
  push:
    branches:
      - "**"

permissions:
  contents: read

jobs:

  i18n-lib-test-build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v5
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: npm
    - name: Install
      run:  npm ci
    - name: Test
      run: npm run test:coverage
    - name: Build Library
      run: ngx ng build ngx-atomic-i18n

  i18n-demo-build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v5
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: npm
    - name: Install
      run:  npm ci
    - name: Build i18n Demo
      run: npm run i18n-build:once:ngx-i18n-demo && ngx ng build ngx-i18n-demo

